<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.smart.home.mapper.AreaMapper">
    <!-- 区域表字段映射 -->
    <resultMap id="BaseResultMap" type="com.smart.home.model.entity.Area">
        <id column="id" property="id" />
        <result column="user_id" property="userId" />
        <result column="area_name" property="areaName" />
        <result column="area_type" property="areaType" />
        <result column="created_at" property="createdAt" />
        <result column="updated_at" property="updatedAt" />
    </resultMap>

    <!-- 基础字段列表 -->
    <sql id="BaseColumns">
        id, user_id, area_name, area_type, created_at, updated_at
    </sql>

    <!-- 根据用户ID查询区域列表 -->
    <select id="selectByUserId" parameterType="java.lang.Long" resultMap="BaseResultMap">
        SELECT
        <include refid="BaseColumns" />
        FROM house_areas
        WHERE user_id = #{userId}
    </select>

    <!-- 根据用户ID和区域名称查询区域 -->
    <select id="selectByUserIdAndName" parameterType="map" resultMap="BaseResultMap">
        SELECT
        <include refid="BaseColumns" />
        FROM house_areas
        WHERE user_id = #{userId} AND area_name = #{areaName}
    </select>
</mapper>